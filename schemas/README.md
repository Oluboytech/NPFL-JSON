# Data schemas

These schemas document the canonical fields added to the existing NPFL JSON
format. They intentionally allow additional properties so existing consumers
and the openfootball-compatible fields are not broken.

The repository's executable validation is in `scripts/validate_data.py` and
uses only Python's standard library.
